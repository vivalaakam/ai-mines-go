# Persistence

Not implemented yet (see TASKS.md Phase 15). Once implemented, this document should
describe the SQLite schema (`saves, levels, chunks, cells, cell_components, workers,
storages, orders, order_requirements`) and the `LoadEngine`/`SaveEngine`/`CreateNewEngine`
adapter contract from `REQUIREMENTS.md` §28-29.
